{
  "name": "productsense",
  "version": "0.0.1",
  "private": true,
  "description": "AI product manager for non-technical founders. Maya + sub-agents + MCP bridge.",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
